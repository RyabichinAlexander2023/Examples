int dogCounter = 0;
int Distance = 1000;
int FirstFriendSpeed = 4;
int SecondFriendSpeed = 5;
int DogSpeed = 10;
int DistanceLimit = 10;
int DogDerection = 1;

while (Distance > DistanceLimit);
speed=0;

    if
    {
      (DogDerection == 1);
       Speed = firstFriendSpeed + DogSpeed;
      (DogDerection = 2) ;
    }
    else
     {
       Speed = SecondFriendSpeed + DogSpeed;
       DogDerection = 1;
       TimeToMeet = (Distance / Speed);
       Distance = distance - TimeToMeet * (FirstFriendSpeed + SecondFriendSpeed);
       DogCounter = DogCounter + 1;
     }       
Console.Write ("distance =" + distance);
Console.WriteLine ("DogCounter =" + DogCounter);